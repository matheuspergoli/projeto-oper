/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ['placehold.co', 'loremflickr.com', 'avatars.githubusercontent.com']
	}
}

export default nextConfig
